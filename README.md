# Encrypted UDP by public private key based single server and 2 clients (sensor and actuator) chat program written in C-language
```
gcc .\server.c -lws2_32 -o server
gcc .\sensor.c -lws2_32 -o sensor
gcc .\actuator.c -lws2_32 -o actuator
```
